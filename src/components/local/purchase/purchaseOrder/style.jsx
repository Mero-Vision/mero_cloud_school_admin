import { makeStyles } from "@mui/styles";

const styles = makeStyles((theme) => ({
  inputSelect: {
    "& select": {
      cursor: "pointer",
      background: "#f6f6f6",
      border: "none",
      outline: "none",
    },
  },
}));

export default styles;
